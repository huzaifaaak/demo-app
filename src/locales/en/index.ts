import { createTranslation } from '@locales/create-translation';

import { Content } from './content';
import { Errors } from './errors';

export default createTranslation({ errors: Errors, content: Content });

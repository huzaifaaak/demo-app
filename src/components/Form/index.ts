import { Form as Wrapper } from './Form';
import { FormError as Error } from './FormError';
import { FormSubmit as Submit } from './FormSubmit';

export const Form = {
    Wrapper,
    Submit,
    Error,
};
